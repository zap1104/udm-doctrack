# UDM DocTrack — Python dependencies
# Install with:  pip install -r requirements.txt

# --- Core ------------------------------------------------------------------
Django==5.0.9
psycopg[binary]==3.2.3          # PostgreSQL driver
python-dotenv==1.0.1            # reads the .env file
whitenoise==6.7.0               # serves static files in production

# --- Security --------------------------------------------------------------
argon2-cffi==23.1.0             # password hashing (stronger than the default)
django-axes==6.5.1              # locks accounts after repeated failed sign-ins
django-csp==3.8                 # Content-Security-Policy headers

# --- Background jobs (no Redis needed) -------------------------------------
django-q2==1.7.2

# --- Text extraction -------------------------------------------------------
pypdf==5.0.1                    # text layer of digital PDFs
python-docx==1.1.2              # .docx files
openpyxl==3.1.5                 # .xlsx files

# --- File storage (only needed when STORAGE_BACKEND is not "local") --------
django-storages[s3]==1.14.4     # Cloudflare R2 / any S3-compatible bucket
# django-storages[azure]==1.14.4  # uncomment instead if you use Azure Blob

# --- Production server -----------------------------------------------------
gunicorn==23.0.0
